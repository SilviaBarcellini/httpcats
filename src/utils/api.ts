/* export type APICAT = {
  id: number;
  name: string;
  image: string;
  url: string;
  created: string;
}; */

/* export type APICats = {
  info: {
    count: number;
    pages: number;
    next: string;
    prev: string | null;
  };
  results: APICAT[];
};

export type cat = {
  imgSrc: string;
  name: string;
};
 */
